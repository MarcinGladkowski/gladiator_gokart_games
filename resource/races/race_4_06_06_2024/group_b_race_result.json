It looks like the race session data wasn't included in your message — the **Data** section appears to be empty. Could you please paste the actual race session data (e.g., CSV, JSON, table, or plain text) so I can extract the results for you?